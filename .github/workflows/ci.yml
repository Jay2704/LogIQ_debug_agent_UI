name: Frontend CI

on:
  push:
    branches:
      - main
      - develop
      - uat
      - 'release/*'
  pull_request:
    branches:
      - main
      - develop
      - uat
      - 'release/*'

jobs:
  build_and_lint:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Run ESLint
        run: npm run lint

      - name: Build project
        run: npm run build
